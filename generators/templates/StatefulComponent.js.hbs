import React from 'react';

import { Stretch } from './Layout';
import { Description, Bold } from './Text';
import { InputField } from './Pepperoni';

export default class {{ properCase name }} extends React.Component {
  // Initial state
  state = {
    value: "I'm a stateful component!",
  };

  onChangeText = text => this.setState({ value: text });

  render = () =>
    <Stretch>
      <Description>
        You typed: <Bold>{this.state.value}</Bold>
      </Description>
      <InputField value={this.state.value} onChangeText={this.onChangeText} />
    </Stretch>;
}
