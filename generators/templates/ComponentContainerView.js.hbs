import React from 'react';

import { Description, Bold } from '../../components/Text';
import { ViewContainer, Padding } from '../../components/Layout';
import {{ properCase name }} from '../../components/{{ properCase name }}';

export default class {{ properCase name }}View extends React.Component {
  static navigationOptions = {
    title: '{{ properCase name }}',
  };

  renderDescription = () =>
    <Description>
      Here's a sample view using your <Bold>{{ properCase name }}</Bold> component.
    </Description>;

  render = () =>
    <ViewContainer>
      <Padding>
        <{{ properCase name }} />
      </Padding>
      {this.renderDescription()}
    </ViewContainer>;
}
